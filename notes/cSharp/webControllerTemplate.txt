using Microsoft.AspNetCore.Mvc;
namespace <ProjectName>.Controllers;     //be sure to use your own project's namespace!
    public class <ControllerName> : Controller   //remember inheritance??
    {
        //for each route this controller is to handle:
        [HttpGet]       //type of request
        [Route("")]     //associated route string (exclude the leading /)
        public string Index()
        {
            return "Hello World from HelloController!";
        }
    }

// NOTES: multiple controllers can have issue of same names for functions